/**
 * LSWrapper is a simple wrapper around LocalStorage.
 */

import * as defs from './defs';
import * as settings from './settings';
import * as utils from './utils';

export { defs, settings, utils };
